apiVersion: apps/v1
kind: Deployment
metadata:
  name: catalog-db-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: catalog-db
  template:
    metadata:
      labels:
        app: catalog-db
    spec:
      containers:
      - name: postgres
        image: postgres:15
        env:
        - name: POSTGRES_USER
          value: "admin"
        - name: POSTGRES_PASSWORD
          value: "password123" 
        - name: POSTGRES_DB
          value: "catalog_db"
        ports:
        - containerPort: 5432
---
apiVersion: v1
kind: Service
metadata:
  name: catalog-db
spec:
  ports:
  - port: 5432
    targetPort: 5432
  selector:
    app: catalog-db